{
  "name": "my-first-edge-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev --compatibility-date=2023-08-06 -- vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "tsc && vite build",
    "deploy": "wrangler pages deploy pages"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230801.0",
    "typescript": "^5.1.6",
    "vite": "^4.4.8",
    "wrangler": "^3.4.0"
  },
  "dependencies": {
    "hono": "^3.3.4",
    "striptags": "^3.2.0"
  }
}
