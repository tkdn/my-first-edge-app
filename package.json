{
  "name": "my-first-edge-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev --local -- vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "tsc && vite build",
    "deploy": "wrangler pages publish pages"
  },
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^4.1.0",
    "wrangler": "^2.12.2"
  },
  "dependencies": {
    "hono": "^3.0.3"
  }
}
